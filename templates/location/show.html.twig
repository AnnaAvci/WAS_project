{% extends 'base.html.twig' %}

{% block title %}Location info{% endblock %}

{% block body %}
    <div class="wrapper">
            <h1 class="page-title">{{location}}</h1>
             {# <a href="{{path('message-to-location', {'id':location.id}) }}"> Send a message to the owner </a> #}


            <div class="all-cards">
                    {% for photoLocation in location.photoLocations %}
                        <div class="card">
                            <img src={{ asset("img/" ~ photoLocation.namePhoto )}} width="300" height="400" alt>
                        </div>
                    {% endfor %}
                </div>
        

                {# All information including comments #}
                <div class="post-info">
                <p>
                <strong>{{location.priceLocation}} $ / hour</strong> 
                </p>
                <p>
                    <img src={{ asset("img/" ~ location.owner.pictureUser )}} width="30" height="30" alt class="profile-picture">
                    <strong>{{location.owner}}</strong> <br>
                </p>
                <p> 
                {{location.description}}
                </p>
                
                <div class="post-comments">
                <h2>Reviews:</h2>
                {% for commentUserLocation in location.commentUserLocations %}
                   
                    {% if not commentUserLocation.commenter %}
                        <strong>Deleted account</strong>
                    {% else %}
                         <img src={{ asset("img/" ~ commentUserLocation.commenter.pictureUser )}} width="30" height="30" alt class="profile-picture">
                         <strong>{{commentUserLocation.commenter.firstName}}</strong>
                    {% endif %}
                    
                    {{commentUserLocation.dateComment | date('d-m-Y H:i')}} <br>
                    <div class="flex">
                        {{commentUserLocation.textComment}} <br>
                        {% if app.user and app.user.id == commentUserLocation.commenter.id %}
                            <a href="{{path('delete_comment', {'id':commentUserLocation.id}) }}"><i class="fa-solid fa-xmark"></i> </a>
                        {% endif %}
                    </div>
                {% endfor %}
                {% if app.user %}
                    <strong>Add comment:</strong>
                        {{form(formCommentUserLocation)}} <br>
                    <strong>Book this location:</strong>
                        {{form(formLocationBook)}} 
                {% endif %}

                
                </div>
            </div>
    </div>
{% endblock %}